# Changelog

Fortlaufendes Log aller Änderungen — auch solche, die NICHT im Code landen
(Command-Registrierungen, Coolify-Env-Änderungen, Server-Eingriffe).
Neueste Einträge oben. Format: `## YYYY-MM-DD`, ein Bullet pro Änderung,
mit Bereich (`music-bot:`, `soundboard-bot:`, `website:`, `infra:`).

## 2026-07-21

- **music-bot:** **Web-Player-Favicon rund gemacht** (Wunsch von Luca — vorher quadratisch). Der bisherige Favicon `beatbyte-avatar.png` (512×512, dunkler Verlauf **bis in die Ecken**) wirkte im Browser-Tab als Kachel. Neuer runder Favicon `bots/music-bot/app/public/beatbyte-avatar-round.png`: gleiches Motiv, aber per Kreis-Maske (Pillow, 4× Supersampling → antialiaste Kante) die Ecken auf transparent gesetzt. `index.html` `<link rel="icon">` zeigt jetzt auf die runde Datei. **Bewusst NICHT den bestehenden `beatbyte-avatar.png` ueberschrieben** — der wird an 4 Stellen in `src/App.jsx` als eckiges Marken-Logo (Sidebar/Setup) genutzt und soll dort unveraendert bleiben; nur der Favicon-Verweis wurde umgebogen. `dist/` ist gitignored und wird im Docker-Build frisch gebaut (Vite kopiert `public/` mit), daher dort nichts angefasst. Kein Dependency-Change → kein Lockfile-Update. Wird beim naechsten music-bot-Deploy live.

## 2026-07-19

- **docs:** **`CHECKLISTE.md`: top.gg-Vote-Webhooks von „Optional" auf gewollt hochgestuft** (Wunsch von Luca). Der Punkt stand in Phase 2 schon als Einzeiler (`Optional: top.gg-Vote-Webhook einbauen, Votern kleinen Perk geben`) — jetzt als fester Punkt mit Unteraufgaben, damit die Umsetzung nicht bei „irgendwas mit Webhook" anfaengt: Endpoint pro Bot in der **bestehenden** Web-App (music-bot `api.js` :3001, soundboard `web/` :3002 — kein neuer Service), Auth ueber den `Authorization`-Header gegen ein Secret (neue Env `TOPGG_WEBHOOK_AUTH`, in Coolify + lokal mit `MUSIC_`/`SOUNDBOARD_`-Praefix), Eintrag der URL + desselben Secrets im top.gg-Dashboard, Persistenz der Votes in SQLite (top.gg erlaubt alle 12 h eine Stimme), Perk-Definition und die i18n-Pflicht (Vote-Strings in `de.js` **und** `en.js` beider Bots). Abhaengigkeit vermerkt: **EarTastic ist auf top.gg noch nicht gelistet** (404, s. Phase-2-Punkt), dort laesst sich also noch kein Webhook eintragen. Reine Doku-Aenderung, kein Code angefasst.
- **infra:** **Redeploy beider Bots → `#status`-Feature ist scharf.** Schliesst den „OFFEN — Redeploy noetig"-Punkt: `STATUS_CHANNEL_ID` war *nach* dem vorherigen Deploy gesetzt worden, die laufenden Container kannten sie also nicht (Coolify injiziert Env beim Container-Start). **Vor dem Neustart Playback-Check** ueber die Admin-API (`GET /api/guilds`, `x-api-key` = Prod-`API_KEY`): alle **3** BeatByte-Guilds `current: null` und Queue-Laenge 0 → idle, der Restart hat niemandem die Musik abgewuergt. Deploys wieder **sequenziell** (RAM): music-bot `p8dkvnllrd9mxh1b5ke6hl4f` → `finished`, soundboard-bot `b46aqj4kumv0crod8bdjcgv2` → `finished`. **Ergebnis:** `#status` (`1528421307698511952`) hat jetzt genau **2 Nachrichten** (vorher 0) — je ein Embed pro Bot (`🎵 BeatByte` / `🔊 EarTastic`, „🟢 **Online** · aktualisiert `<t:…:R>`"), **keine Duplikate**. Health danach: `beatbyte.bytebots.de/status` HTTP 200 (der erste Versuch gab **502** — das war nur der Entrypoint-Boot mit yt-dlp/POT-Check, nach dem Hochlauf 200), `soundboard.bytebots.de/api/health` `{status:ok}`. **Verifikations-Falle dokumentiert (kein Bug, kostete aber eine Fehlersuche):** Liest man den Kanal per REST aus, liefert Discord `content` **und** `embeds` von Nachrichten **fremder** Autoren leer, solange der lesende Bot den privilegierten *Message-Content*-Intent nicht hat — der ist bei beiden Apps bewusst aus. Mit BeatByte-Token wirkte EarTastics Status-Nachricht daher wie eine kaputte Leernachricht (`content:""`, `embeds:[]`), obwohl sie korrekt ist. Der **Kreuztest mit beiden Tokens** zeigt es symmetrisch: jeder Bot sieht nur sein eigenes Embed. Wer `#status` kuenftig prueft: je Bot mit **dessen eigenem** Token lesen (oder einfach in Discord schauen), sonst diagnostiziert man einen Phantom-Bug. Kanal-Overwrites vorab gegengeprueft (beide managed Bot-Rollen haben VIEW/SEND/EMBED, `@everyone` SEND denied) — Permissions waren nie die Ursache.
- **music-bot:** **DeprecationWarning `ready` behoben** — der Nebenbefund aus dem Deploy-Log (`The ready event has been renamed to clientReady …`). Ursache war einzig der hartcodierte String in `client.once('ready', …)`; soundboard-bot war mit `Events.ClientReady` schon korrekt (daher warnte nur music-bot). Jetzt nutzt music-bot ebenfalls **`Events.ClientReady`** (`Events` dem discord.js-Import ergänzt). **Bewusst der Enum statt eines harten `'clientReady'`:** der Enum löst je nach Version auf den dort gültigen Namen auf — ein hartcodiertes `'clientReady'` würde bei einer älteren gepinnten Version still **nie** feuern und den Bot-Start lahmlegen. Mechanik an der Bibliotheks-Quelle verifiziert (`WebSocketManager.js`): Zeile 386 `if (this.client.emit('ready', …) && !deprecationEmitted)` — `emit()` liefert nur `true`, wenn ein Listener existiert, die Warnung hängt also allein am `'ready'`-Listener; Zeile 401 `this.client.emit(Events.ClientReady, …)` feuert bedingungslos, der Handler läuft also unverändert. Gegengeprüft: `Events.ClientReady === "clientReady"` in der installierten 14.26.2 **und** in der vom Lockfile gepinnten 14.26.0 (= was Prod baut), kein `'ready'`-String-Listener mehr in beiden Bots, `node --check` grün. Rein kosmetisch (die Warnung war kein Fehler), aber v15-fest.
- **infra:** **Command-Neuregistrierung beider Bots erledigt** — schliesst den als „weiterhin offen (2)" markierten Punkt des Deploy-Eintrags unten. Die umlaut-korrigierten Command-Descriptions waren nach dem reinen Code-Deploy bei Discord noch die alten (`Oeffne…`), da Descriptions Registrierungs-Metadaten sind. Global neu registriert per API mit den Prod-Tokens aus der Root-`.env` (Music-Block = erstes, Soundboard-Block = letztes Vorkommen der doppelten Keys), Identitaets-Check `bot.user.id === CLIENT_ID` bestanden, **ohne** `GUILD_ID` → global: **BeatByte 26 Commands** (`/autodj` jetzt „Auto-DJ ein-/ausschalten — spielt automatisch **ähnliche** Songs"), **EarTastic 8 Commands** (`/dashboard` „**Ö**ffne das Soundboard-Dashboard im Browser", `/soundboard` „**Ö**ffnet das Soundboard-Panel", `/volume` „Stelle deine **persönliche Lautstärke** ein"). Discord hat die korrigierten Texte in der PUT-Antwort zurueckgeliefert und ein anschliessender read-only GET bestaetigt sie. **EarTastics guild-scoped Liste weiterhin `0`** → die Entkopplung von 2026-07-17 haelt, keine Duplikate. Global-Propagation bis ~1 h. Damit ist der Umlaut-Fix in allen drei Wirkungsklassen live (Runtime-Strings + Web-Dashboard per Deploy, Descriptions per Registrierung).
- **infra:** **Beide Bots deployed** — Branch-Tip `7aea1c1` (= `#status`-Kanal `a9e6c3f` + Umlaut-Fix `7aea1c1`) nach `origin/coolify-deploy` gepusht und per Coolify-API ausgerollt: `POST /api/v1/deploy` für music-bot `e9kxd4rv…` (Deployment `rwuyg7wn…`) und soundboard-bot `rhyrgoi2…` (Deployment `uuqcva2w…`), **beide `finished`** (soundboard sofort, music-bot ~3 min wegen yt-dlp/FFmpeg/POT + Web-Player-Build). Verifiziert nach dem Deploy: `beatbyte.bytebots.de` HTTP 200, `soundboard.bytebots.de/api/health` HTTP 200 `{"status":"ok"}` mit frischer Uptime; Runtime-Logs zeigen `✅ Bot ist online als BeatByte#3883` + `🌐 API Server läuft auf Port 3001` bzw. `Bot ist online als EarTastic#1257` / `In 2 Server(n)`. **Kein `MODULE_NOT_FOUND`** für den neuen `require('../../../libs/status')` in beiden Images — der `libs/notify.js`-Crashloop vom 2026-07-18 wiederholt sich also nicht (`COPY libs ./libs` greift für die neue Datei genauso). **Kein** `status: Update fehlgeschlagen` im Log — korrekt, denn `STATUS_CHANNEL_ID` ist nirgends gesetzt, der Updater bleibt stiller No-op. Der Umlaut-Fix ist mit live (soundboard-Startlog zeigt jetzt „Web-Dashboard **läuft**"). **Weiterhin offen:** (1) #status-Kanal anlegen + Rechte + `STATUS_CHANNEL_ID` bei beiden Bots setzen, sonst bleibt das Feature dormant; (2) Command-Neuregistrierung für die umlaut-korrigierten Command-Descriptions (`/autodj`, `/dashboard`, `/soundboard`, `/volume`) — der Code-Deploy allein aktualisiert die bei Discord registrierten Beschreibungen nicht.
- **infra + soundboard-bot:** **`#status` ist live — BeatByte laeuft, EarTastic postet eine LEERE Nachricht (offener Bug).** Nach dem Deploy beider Bots (Deployments `qkmduxs2…` + `xu3pgo1i…`, beide `finished`) steht BeatBytes Embed korrekt im Kanal und aktualisiert sich im 5-Minuten-Takt (Server 3 · Aktive Wiedergaben 0 · Web-API Port 3001). EarTastic erzeugt/editiert seine Nachricht ebenfalls im 5-Minuten-Takt, aber mit `embeds: []`. **Eingegrenzt (was es NICHT ist):** (1) *keine Rechte-Frage* — `EMBED_LINKS` steckt sowohl in der Rolle (`36785152`) als auch im Kanal-Overwrite; (2) *kein Code-Unterschied* — derselbe `buildEmbed`+`edit`-Pfad liefert **lokal** mit demselben Prod-Token auf derselben Nachricht `embeds: 1`; die Aufrufstellen beider Bots sind strukturell identisch, discord.js ist bei beiden `14.26.0`; (3) *kein stale Image* — Force-Rebuild ohne Cache, `COPY libs ./libs` lief als Step `#10`, Image getaggt auf `2afb901`. **Der eigentliche Befund:** die API meldet **Erfolg inklusive Embed**, und ~90 s spaeter ist die Nachricht leer — **ohne neuen `edited_timestamp`**. Das Embed wird also *nachtraeglich entfernt*, nicht ueberschrieben; identisch reproduziert bei manuellem `PATCH` (15:45) und beim lokalen discord.js-Test (15:56). **Verdacht (unbewiesen):** AutoMod-Regel „Spam blockieren" (`trigger_type=3`, ML-Filter) — sie hat **keine** `exempt_roles`/`exempt_channels`; dagegen spricht, dass das Audit-Log 0 AutoMod-Eintraege zeigt, und unerklaert bleibt, warum BeatByte verschont bleibt. Naechster Schritt in `CHECKLISTE.md`: #status in der Discord-UI aus der Spam-Regel ausnehmen und erneut messen — mein API-Versuch dazu wurde vom Auto-Mode-Classifier geblockt (Moderationsregeln). Nebenbei gelernt: der Coolify-Log-Endpoint zeigt **nur stdout**, `console.error` bleibt unsichtbar — die Status-Diagnose laeuft deshalb ueber `console.log`. Die neue Diagnose-Ausgabe in `libs/status.js` meldet verworfene Embeds, faengt diesen Fall aber bewusst **nicht** (die API meldet ja Erfolg) — im Kommentar so festgehalten.
- **infra:** **`#status`-Kanal eingerichtet + `STATUS_CHANNEL_ID` in Coolify gesetzt** — der fehlende Nicht-Code-Teil zum `libs/status.js`. Kanal `#status` per Discord-API in der Kategorie 📢 Info angelegt (ID **`1528421307698511952`**), mit drei Permission-Overwrites: `@everyone` = `VIEW_CHANNEL`+`READ_MESSAGE_HISTORY` und `SEND_MESSAGES` **denied** (read-only fuer Besucher), die Rollen `BeatByte`/`EarTastic` = View/Send/Embed/Read-History explizit erlaubt (haetten sie ueber ADMINISTRATOR ohnehin — explizit ist robust, falls das Recht spaeter beschnitten wird, s.u.). Angelegt mit dem Prod-BeatByte-Token, weil der read-only Admin-Bot nur `permissions=1024` hat. Danach `STATUS_CHANNEL_ID=1528421307698511952` bei **beiden** Apps via `POST /applications/<uuid>/envs` gesetzt — bewusst einzeln statt `PATCH /envs/bulk`, um bestehende Variablen nicht zu gefaehrden; verifiziert per Key-Zahl (music-bot 10→11, soundboard 12→13). Stolperstein: `POST /envs` lehnt `is_build_time` mit **422** ab (`"This field is not allowed."`) — nur `key`/`value`/`is_preview`/`is_literal` senden. **OFFEN — Redeploy noetig:** der Deploy weiter oben lief zeitlich **vor** dieser Env-Aenderung, die laufenden Container kennen `STATUS_CHANNEL_ID` also nicht; per API nachgeprueft: `GET /channels/1528421307698511952/messages` liefert **0 Nachrichten**, `status.js` ist noch No-op. Mein eigener Deploy-Versuch wurde vom Auto-Mode-Classifier geblockt → muss von Luca in Coolify ausgeloest werden. Vorher geprueft, dass ein Neustart niemanden stoert: ueber die Admin-API meldeten **alle 3** BeatByte-Guilds `connected:false`, keine Wiedergabe, leere Queue.
- **infra:** **Bot-Rechte auf dem Support-Server auf Least Privilege reduziert (teilweise).** Beide Bots hatten dort **ADMINISTRATOR**. Ziel-Set aus dem Code hergeleitet statt geraten: `channel.send()` + Embeds (`SEND_MESSAGES`+`EMBED_LINKS`), `msg.delete()` ausschliesslich auf **eigenen** Nachrichten (braucht **kein** `MANAGE_MESSAGES`), Voice-Playback (`CONNECT`+`SPEAK`+`USE_VAD`), `messages.fetch` in `libs/status.js` (`READ_MESSAGE_HISTORY`), keine Attachments (`AttachmentBuilder` kommt in keinem Bot vor) → Bitmaske **`36785152`**. **Wichtiger Zwischenschritt zuerst:** die Kategorien 🎵 BeatByte / 🔊 EarTastic verbieten `@everyone` `VIEW_CHANNEL`, und die Bot-Rollen hatten dort **keine** eigenen Overwrites — sie sahen diese Kanaele also nur dank ADMIN. Ohne Vorarbeit haette die Demotion die Bots aus ihren eigenen Support-Kanaelen ausgesperrt. Darum per `PUT /channels/<id>/permissions/<role>` je Bot-Rolle auf **ihrer eigenen** Kategorie + deren 3 Kanaelen VIEW/SEND/EMBED/HIST gesetzt (8 Overwrites, jeder Bot sieht bewusst nur die eigene Kategorie). **Erledigt: EarTastic** — Rolle steht auf `36785152`, ADMIN weg, Bot weiter gesund (`/api/health` 200, Uptime laeuft durch). **OFFEN: BeatByte** — per API nicht machbar, weil BeatByte mit **pos 8 die hoechste Rolle** ist und Discord das Bearbeiten der eigenen hoechsten Rolle verbietet; EarTastic (pos 7) steht darunter und hat nach seiner Demotion ohnehin kein `MANAGE_ROLES` mehr. Muss Luca in der Discord-UI machen (Schritte in `CHECKLISTE.md`). Ebenfalls offen: `install_params.permissions` steht bei beiden Apps auf `0` — der Invite-Link sollte auf dasselbe Set eingekuerzt werden.
- **infra:** **Terms-of-Service- und Privacy-Policy-URL im Developer Portal gesetzt** (von Luca) — per `GET /applications/@me` bei **beiden** Apps verifiziert: `terms_of_service_url` = `https://bytebots.de/nutzungsbedingungen`, `privacy_policy_url` = `https://bytebots.de/datenschutz`. Damit ist die Voraussetzung fuer Phase 5 (App Directory) erfuellt; `CHECKLISTE.md` abgehakt.
- **docs:** **`CHECKLISTE.md` gegen den echten Ist-Zustand abgeglichen** (statt nach Gedaechtnis abgehakt) — drei read-only Quellen: `npm run inspect:discord` (Support-Server), `GET /applications/<uuid>/envs` (Coolify, beide Bots) und `GET /applications/@me` + `/commands` mit den Prod-Bot-Tokens. **Neu abgehakt:** (1) *Prod-Command-Deploy* — global registriert sind BeatByte 26 Commands / EarTastic 8, inkl. `/support`, `/invite` und `/language`; der Vermerk „noch Prod-`npm run deploy`" am 2026-07-14-Punkt war veraltet. (2) *Privileged Intents pruefen* — bei beiden Apps ist **keiner** gesetzt (BeatByte `flags 10485760`, EarTastic `8388608`; weder `GATEWAY_MESSAGE_CONTENT(_LIMITED)` noch `GUILD_MEMBERS(_LIMITED)`/`PRESENCE(_LIMITED)`), es gibt also nichts abzuschalten. (3) *top.gg* auf `[~]` gesetzt: **BeatByte ist gelistet** (`top.gg/bot/1488919318472298647` → 200, passend zur neuen Top.gg-Rolle auf dem Support-Server), **EarTastic nicht** (404). **Als weiterhin offen verifiziert** (nicht nur vermutet): `#status`-Kanal existiert nicht (16 Kanaele, keiner heisst `status`) und `STATUS_CHANNEL_ID` ist in Coolify bei **keinem** Bot gesetzt → `libs/status.js` laeuft aktuell als No-op; Privacy-Policy-/ToS-URL sind bei **beiden** Apps `null`; discordbotlist.com listet keinen der Bots (404/404); Support-/Ankuendigungs-Kanaele sind weiter Typ *Text* (keine Forum-/News-Kanaele); Banner+Splash weiter durch 0 Boosts gesperrt. Ausserdem als Kontext in Phase 4 notiert: **BeatByte 3 Server, EarTastic 2** (`approximate_guild_count`) — bis zur 75er-Verifizierungsschwelle ist Phase 2/3 der Hebel. Reine Doku-Aenderung, kein Code angefasst.
- **music-bot + soundboard-bot:** **`#status`-Kanal** gebaut (Monitoring-Folgepunkt, nutzt den Alert-Weg von 2026-07-17). Neues geteiltes `libs/status.js` (`startStatusUpdater({ client, botName, emoji, getState })`): jeder Bot pflegt **genau EINE eigene Nachricht** im #status-Kanal (`🎵 BeatByte` / `🔊 EarTastic`, grünes Embed mit „🟢 **Online** · aktualisiert `<t:…:R>`", Server-Zahl + Web-Port), die alle **5 Min** neu geschrieben wird. Bei einem (Re-)Start wird die **vorhandene eigene Nachricht wiederverwendet** (per Author-ID im Kanal gesucht, `messages.fetch` limit 50) statt eine neue zu posten → **keine Karteileichen**. Der relative Zeitstempel ist zugleich das Liveness-Signal: aktualisiert sich die Zeit nicht mehr, ist der Bot tot. Aktivierung nur via **`STATUS_CHANNEL_ID`** (Kanal-ID), **No-op ohne die Var** — analog `notify.js`. Bewusst **kein Webhook** (Webhooks können ihre eigenen Nachrichten nicht wiederfinden → Restart-Duplikate); über den Bot-Client geht das per Author-ID. `setInterval` mit **`.unref()`** (die Lehre aus `d8bb321`, sonst hängt `npm run deploy`). Kein Message-Content-Intent nötig (nur Autor-ID der eigenen Nachricht). Eingehängt in beide `ready`-Handler: music-bot meldet Guild-Zahl + aktive Wiedergaben (`[...queues.values()].filter(q=>q.current)`) + API-Port; soundboard Guild-Zahl + Web-Dashboard-Port. Verifiziert gegen einen Fake-Channel (9/9 Checks PASS): ohne Env kein Post, Erst-Start postet genau 1 Nachricht mit korrektem Titel/Feldern, **Restart editiert die bestehende statt zu duplizieren**, Offline-State → rotes Embed; `node --check` aller drei Dateien grün. `.env.local.example` um `MUSIC_`/`SOUNDBOARD_STATUS_CHANNEL_ID` ergänzt. Kein Dependency-Change → kein Lockfile-Update. **Offen (kein Code):** #status-Kanal auf dem Support-Server anlegen, beiden Bots dort View/Send/Embed/Read-History-Recht geben, `STATUS_CHANNEL_ID` (gleiche ID) in Coolify bei beiden Bots setzen — sonst No-op. `CHECKLISTE.md` (Monitoring- + Phase-1-`#status`-Punkt) abgehakt.

## 2026-07-18

- **music-bot + soundboard-bot:** **Transliterierte Umlaute (ae/oe/ue) in nutzersichtbaren DE-Texten auf echte Umlaute korrigiert.** Die CLAUDE.md-Regel „Umlaute als ae/oe/ue" gilt nur fuer **Commit-Messages** — in der UI war es schlicht falsch geschrieben (der Rest der i18n-Kataloge macht es korrekt, z.B. `/app` „Öffne den BeatByte Music Player"). Gefunden beim Read-only-Check des Prod-Registrierungsstands (`/language` + Localizations sind bereits live). **28 Ersetzungen in 7 Dateien**, drei Wirkungsklassen: (1) **Discord-i18n-Kataloge** `de.js` — music-bot `app.desc`/`playlist.invalidUrl`; soundboard `sb.desc.all`/`sb.empty`/`sb.categoryPlaceholder`/`sb.rateLimit`/`sb.favAdded`/`favorite.added`/`volume.set`/`dashboard.btn` → Runtime, greifen nach **Code-Deploy**. (2) **Command-Descriptions** (`setDescription`) — music-bot `/autodj` (`aehnliche`→`ähnliche`), soundboard `/dashboard`/`/soundboard`/`/volume` → brauchen Code-Deploy **und Command-Neuregistrierung**. (3) **Soundboard-Web-Dashboard** `web/server.js` — 8 API-Fehlermeldungen (`Ungueltige…`/`…geloescht`) + 1 Startup-Log → Runtime, nach Deploy. Verifiziert offline: Re-Scan 0 Transliterationen, `node --check` 7/7, beide Kataloge laden mit DE/EN-Paritaet (83/83, 35/35), `toJSON` der 4 geaenderten Builder zeigt korrekte Umlaute. Kein Dependency-Change → kein Lockfile-Update. **OFFEN (outward-facing, nicht automatisch ausgefuehrt):** Code-Deploy beider Bots via Coolify + danach Command-Neuregistrierung (music-bot global; soundboard global — dank Scope-Entkopplung jetzt via `npm run deploy` mit auskommentiertem `SOUNDBOARD_COMMANDS_GUILD_ID` moeglich).
- **soundboard-bot:** **Alerting-Anbindung nachgereicht** — der 2026-07-17-Eintrag beschrieb das Discord-Alerting fuer *beide* Bots, tatsaechlich committet war aber nur die music-bot-Seite: die `notify`-Zeilen in `bots/soundboard-bot/src/index.js` lagen noch untracked im Working-Tree (Kehrseite desselben Staging-Fehlers, der den music-bot-Crashloop ausgeloest hat — dort wurde eine halbfertige Zeile ZU viel mitgenommen, hier eine fertige zu wenig). Jetzt eingehaengt: `notifyOnline('EarTastic', …)` im `ClientReady` sowie `notifyError('EarTastic', …)` an `unhandledRejection`, `uncaughtException` und `Events.Error`. Verifiziert: `libs/notify.js` ist seit `2ac847d` getrackt und wird vom soundboard-Dockerfile via `COPY libs ./libs` ins Image gelegt (Require-Pfad `../../../libs/notify` loest auf), `require()` liefert beide Funktionen, `node --check` gruen — der music-bot-MODULE_NOT_FOUND kann sich hier also nicht wiederholen. Kein Dependency-Change → kein Lockfile-Update. **Deployed** (Coolify `kd9gyzvj…`, `finished`): `soundboard.bytebots.de/api/health` HTTP 200 `{status:ok}` mit frischer Uptime, Log zeigt `Bot ist online als EarTastic#1257` — **kein Crashloop**, der `libs/notify`-Require greift im Image.
- **infra:** **Alerting ist scharf — `ALERT_WEBHOOK_URL` war entgegen der Notiz vom 2026-07-17 bereits gesetzt.** Beim Deploy-Verify per `GET /applications/<uuid>/envs` geprueft: die Var existiert bei **beiden** Bots (music-bot `e9kxd4rv…` + soundboard `rhyrgoi2…`), der Punkt war also nur in CHANGELOG/CHECKLISTE noch als offen gefuehrt. Damit ist das Alerting kein No-op mehr. Indirekt bestaetigt: nach dem soundboard-Restart steht im Runtime-Log **kein** `notify: Webhook-Post fehlgeschlagen` (die einzige Fehlerausgabe des fire-and-forget-Posts) → der `🟢 online`-Ping ging durch. **Einschraenkung:** der `/envs`-Endpoint gibt Werte nie zurueck, geprueft ist also die *Existenz* des Keys, nicht die Gueltigkeit der Webhook-URL. `CHECKLISTE.md` entsprechend abgehakt.
- **docs:** `CHECKLISTE.md` um den Abschnitt **„Monitoring & Alerting"** ergaenzt: Discord-Alerting abgehakt `[x]`, offen bleiben das Setzen von `ALERT_WEBHOOK_URL` in Coolify (der Schritt, der es scharf schaltet), ein periodischer `/play`-Selbsttest, der `#status`-Kanal aus den Web-APIs (Dublette zum Phase-1-Punkt) und ein einmaliger Backup-Restore-Test der taeglichen `data/backups/` inkl. Mini-Runbook.

## 2026-07-17

- **infra:** **i18n beider Bots deployed — mit Incident + Hotfix.** Beide per Coolify deployed (music-bot `e9kxd4rv…`, soundboard `rhyrgoi2…`), beide Deploys `finished`. **soundboard sofort gesund** (HTTP 200). **music-bot crashte im Loop** (`restarting:unknown`, 503). Ursache per SSH-`docker logs` (Luca einmalig freigegeben): `Error: Cannot find module '../../../libs/notify'`, ExitCode 1, **kein OOM** (649 MB frei) — NICHT der i18n-Code (lokaler Boot des music-bot lief sauber → prod-spezifisch eingegrenzt) und NICHT RAM. **Grund:** der i18n-Commit `a6b9d52` hat via `git add bots/music-bot/src` die schon im Working-Tree stehende `notify`-Require-Zeile (Parallelarbeit) mit-committet, aber `libs/notify.js` lag als untracked Datei ausserhalb der geadd-ten Pfade → kaputter Verweis im Image (soundboard bekam den notify-require erst nach dem Commit → nicht committet → lief durch). **Hotfix `2ac847d`:** `libs/notify.js` nachgereicht (self-contained, module-load-safe, No-op ohne `ALERT_WEBHOOK_URL`) + music-bot-Redeploy → **wieder online** (BeatByte#3883, API 3001, HTTP 200). **Lehre:** `git add <verzeichnis>` zieht bei parallelen Baustellen fremde/halbfertige Working-Tree-Zeilen mit — Staging-Liste vor dem Commit genauer prüfen. **Command-Neuregistrierung danach erledigt:** beide Bots GLOBAL per `npm run deploy` (Prod-Token injiziert, Guild-Scope leer: music `GUILD_ID=`, soundboard `COMMANDS_GUILD_ID=`; vorher Token→Bot-Identität via `/users/@me` geprüft). Verifiziert mit `?with_localizations=true`: **BeatByte 26 Commands, alle mit en-US-Description**; **EarTastic 8, alle mit en-US-Description**; `/language` bei beiden; **0 guild-scoped Duplikate** in der Haupt-Guild (Entkopplung wirkt). Propagation bei Discord bis ~1 h. **Kleiner Follow-up:** `soundboard-bot npm run deploy` **hängt nach erfolgreicher Registrierung** (Prozess bleibt offen), weil `soundboard.js` → `handlers/interactionHandler.js` ein `setInterval` (Rate-Limiter-Cleanup) registriert → `node` beendet sich nicht. PUT läuft davor durch (Registrierung ok). **Behoben:** `.unref()` auf dem Rate-Limiter-`setInterval` in `handlers/interactionHandler.js` — der Timer haelt den Node-Prozess nicht mehr offen, `npm run deploy` beendet sich sauber (verifiziert: Require-Kette laedt, Prozess exit 0). Harmlos fuer den laufenden Bot (Discord-Client haelt den Event-Loop ohnehin offen); greift beim naechsten soundboard-Deploy, fuer lokales `npm run deploy` sofort.
- **music-bot + soundboard-bot:** **Fehler-/Crash-Alerting nach Discord** eingebaut — Prozess-/Client-Fehler liefen bisher nur in `console.error` → Coolify-Logs, die niemand aktiv beobachtet (bei einem Prod-Crash erfuhr man es erst vom Nutzer). Neues geteiltes `libs/notify.js` (`notifyError(bot, title, err, ctx)` + `notifyOnline(bot, detail)`) postet als Discord-Embed an einen Webhook aus **`ALERT_WEBHOOK_URL`**; **No-op ohne die Env-Var** (lokal/Dev bleibt still), natives `fetch` (Node 22) → keine neue Dependency. Eingehängt in beiden Bots an `unhandledRejection`, `uncaughtException` und `client error` (rot) sowie am `ready`-Event ein **`🟢 online`-Ping** (= zuverlässigstes Crash-Signal, da der Container nur nach einem Absturz neu hochfährt). Gegen Log-Fluten: **Dedup** identischer Meldungen (60s) + globaler Deckel (max. 5 Alerts/60s); der Webhook-Post ist fire-and-forget und kann den Bot nie stören. Verifiziert gegen einen lokalen Fake-Webhook: ohne Env kein Post, `online`-Ping + Fehler-Embed mit Kontext+Stack, identischer Zweitfehler deduped (PASS); `node --check` beider `index.js` + `notify.js` grün. `.env.local.example` um `MUSIC_`/`SOUNDBOARD_ALERT_WEBHOOK_URL` ergänzt. Kein Dependency-Change → kein Lockfile-Update. **Offen (kein Code):** in Coolify je Bot `ALERT_WEBHOOK_URL` auf einen (privaten) Discord-Webhook setzen — sonst bleibt das Alerting inaktiv. `CHECKLISTE.md` (neuer Abschnitt „Monitoring & Alerting") aktualisiert.
- **soundboard-bot / infra:** **Command-Scope dauerhaft von `GUILD_ID` entkoppelt** — die 2026-07-16-FALLE ist damit behoben. `libs/deploy-commands.js` liest die Scope-Guild jetzt aus einer per Option wählbaren Env-Variable (`deployCommands(dir, name, prefix, { guildIdVar })`, Default weiter `GUILD_ID` → **music-bot unverändert**). `bots/soundboard-bot/src/deploy-commands.js` nutzt `guildIdVar: 'COMMANDS_GUILD_ID'`: lokal in `.env.local` gesetzt (Dev-Guild → Commands sofort sichtbar), in Prod/Coolify **nicht** gesetzt → **GLOBAL**. Das Laufzeit-`GUILD_ID` (Dashboard-Login, `web/auth.js`) bleibt unberührt. **Folge für den offenen Infra-Punkt unten:** soundboard-Commands können jetzt wieder normal per `npm run deploy` (mit **Prod**-Token + `SOUNDBOARD_COMMANDS_GUILD_ID` in `.env.local` auskommentiert, analog `MUSIC_GUILD_ID`) global registriert werden — der manuelle `PUT /applications/<clientId>/commands`-Workaround ist nicht mehr nötig. Dev-Var `SOUNDBOARD_COMMANDS_GUILD_ID` in `.env.local` + `.env.local.example` ergänzt, CLAUDE.md-Deployment-Abschnitt + Memory `soundboard-commands-global` aktualisiert. Verifiziert (Dummy-Token → kein Prod-Zugriff): `COMMANDS_GUILD_ID` leer → Route `.../commands` (GLOBAL) trotz gesetztem `GUILD_ID`; `COMMANDS_GUILD_ID=<id>` → Route `.../guilds/<id>/commands`; `node --check` beider Dateien grün. Kein Dependency-Change → kein Lockfile-Update.
- **music-bot + soundboard-bot:** **Beide Bots auf Deutsch + Englisch (i18n) umgestellt** — der bytebots.de-„mehrsprachig"-Claim ist damit real (Ziel auf Luca-Wunsch bewusst auf **DE+EN** begrenzt, kein offener Sprachen-Fahrplan). **Geteiltes Fundament** `libs/i18n.js`: `createI18n(catalog)` → `t(key, locale, vars)` (Katalog-Werte sind Strings mit `{var}` ODER Funktionen für Plural/Bedingung; Fallback Ziel-Sprache → DE → Key, crasht nie) + `normalizeLocale` (de*→de, sonst en). Pro Bot ein Katalog `src/i18n/de.js`+`en.js` (music-bot **83** Keys, soundboard **35**, DE=EN-Parität geprüft). **Sprachwahl pro Server:** neuer **`/language`**-Command (nur „Server verwalten"-Recht; Choices Deutsch/English; Bestätigung in der gewählten Sprache) + neue Spalte `guild_settings.language` (music-bot per `ALTER TABLE`-Migration, soundboard neue Tabelle + Getter/Setter). Auflösung: Einstellung > `interaction.guildLocale` > User-`locale` > `de`; geteilte/Hintergrund-Nachrichten (Now-Playing) nutzen die Server-Einstellung. **Discord-Command-UI zweisprachig:** `setDescriptionLocalizations` an allen Command- + Options-Beschreibungen beider Bots (Command-*Namen* bleiben englisch), `setNameLocalizations` bei der `/language`-Option (`sprache`→`language`) + loop/filter-Choices. Verifiziert offline/integrativ: Syntax aller Dateien, Katalog-Parität, alle Command-Builder `toJSON`, `t()` DE/EN inkl. Plural/Interpolation, Locale-Fallback-Kette, **DB-Migration + `guild_settings.language`-Round-Trip** beider Bots (init→set→get→reset). **Noch NICHT deployed / Commands noch nicht neu registriert** — s. eigener Eintrag unten.
- **music-bot:** Runtime-i18n in allen **25 Commands** + `utils/checks.js` (`requireVoiceChannel`/`requirePlaying` bekommen `locale`-Param, Default DE) + Button-Handler + globalem Error-Handler + **Now-Playing-/Loading-Embed** (`buildNowPlayingEmbed`/`buildLoadingEmbed` bekommen `loc`; das Now-Playing-Embed war vorher für ALLE hart englisch → jetzt DE für DE-Server; `queue.guildId` ergänzt, damit Hintergrund-Updates die Server-Sprache via neuem `guildLocaleFor()` auflösen). **Bewusst DE gelassen:** ~20 provider-spezifische Wurf-Fehler im Such-/Streaming-Stack (`throw new Error('Ungültige Spotify-URL'|'Keine Ergebnisse'…)`) — die zu übersetzen hieße `locale` durch den ganzen Such-Stack zu fädeln (reine Fehlerpfade, Follow-up).
- **soundboard-bot:** Runtime-i18n in allen **7 Commands** + `handlers/interactionHandler.js` (Panel `buildSoundboardPanel` bekommt `loc`; Titel/Beschreibungen/Footer/Select-Menü/Buttons + alle Button-Aktionen übersetzt). **Bugfix nebenbei:** der Refresh-Button leitete die aktive View aus dem **übersetzten Embed-Titel** ab (`title.includes('Alle')`) — das wäre auf Englisch gebrochen; jetzt steckt die View sprachunabhängig in der customId (`sb_refresh_<view>`). Da soundboard kein `ctx` durchreicht, bringt `src/i18n/index.js` die Locale-Auflösung selbst mit (`localeFor(interaction)`, lazy `require('../database')` gegen Modul-Zyklus).
- **infra (OFFEN — bewusst nicht automatisch ausgefuehrt, outward-facing an 2 Prod-Bots):** (1) **Code-Deploy** beider Bots via Coolify (music-bot uuid `e9kxd4rv94k3vu3at3nw6o7u`, soundboard `rhyrgoi2u2l1f0g7xyp6c0m7`). (2) **Command-Neuregistrierung** für die neuen Beschreibungen + `/language`: **music-bot** global (`npm run deploy` mit **Prod**-Token, dabei `MUSIC_GUILD_ID` in `.env.local` auskommentieren — sonst guild-scoped). **soundboard-bot NICHT per `npm run deploy`** (registriert wegen immer gesetztem `GUILD_ID` guild-scoped → Duplikate in der Haupt-Guild, s. Eintrag 2026-07-16) — stattdessen **global per API** (`PUT /applications/<clientId>/commands`, Prod-EarTastic-Token, OHNE GUILD_ID). Ohne Neuregistrierung greift die Runtime-Übersetzung nach dem Deploy zwar sofort, aber Command-Beschreibungen/`/language` erscheinen erst nach der Registrierung (Global-Propagation bis ~1 h).

## 2026-07-16

- **soundboard-bot:** EarTastics Slash-Commands von **guild-scoped auf GLOBAL** umgestellt. **Warum:** Auf dem Support-Server (und jedem Server ausser der Haupt-Guild) tauchten EarTastics Commands nicht im `/`-Menue auf, obwohl der Bot dort praesent/online ist. Ursache: `libs/deploy-commands.js` registriert bei gesetztem `GUILD_ID` guild-scoped — und soundboard-bot hat `GUILD_ID` (fuer den Dashboard-Login-Check, `web/auth.js`) **immer** gesetzt, anders als music-bot (global, weil ohne `GUILD_ID`). **Wie:** Einmal-Skript (Scratchpad, geloescht), Prod-EarTastic-Token/`CLIENT_ID`/`GUILD_ID` aus der Root-`.env` (Soundboard-Block = letztes Vorkommen bei doppelten Keys); Sicherheits-Check `bot.user.id === CLIENT_ID` bestanden. `PUT /applications/<clientId>/commands` → **7 Commands global** (`/dashboard`, `/favorite`, `/invite`, `/sound`, `/soundboard`, `/support`, `/volume`), 200 OK; danach `PUT /applications/<clientId>/guilds/879650782629199893/commands` mit `[]` → guild-scoped geleert (200 OK), sonst Duplikate in der Haupt-Guild. Global-Propagation dauert bei Discord bis ~1 h. **FALLE fuer spaeter:** `npm run deploy` in `bots/soundboard-bot` registriert wegen des immer gesetzten `GUILD_ID` wieder **guild-scoped** → wuerde die globalen Commands nicht ersetzen, sondern in der Haupt-Guild Duplikate erzeugen. Bis der Deploy-Pfad entkoppelt ist (eigener Scope-Schalter statt `GUILD_ID`): soundboard-Commands NICHT per `npm run deploy` neu registrieren, sondern global (beim Registrieren `GUILD_ID` weglassen). Kein Code-Change, reine Registrierungs-Aktion.
- **infra:** Onboarding-Kanal-Gating **end-to-end verifiziert** + Admin-Bot **auf read-only zurueckgesetzt**. (1) Rechte-Aufloesung nach Discords Algorithmus auf den **echten Unterkanal-Overwrites** (nicht nur Kategorie): die 6 Bot-Kanaele haben nach dem Kategorie-PUT je 4 eigene Overwrites (synchronisiert) → `@everyone` versteckt, `@BeatByte 🎵` sieht nur BeatByte, `@EarTastic 🔊` nur EarTastic; echtes Mitglied (Top.gg-Bot) korrekt ausgesperrt. (2) **Live-Enforcement** gegen die Discord-API (nachdem Luca dem Admin-Bot Administrator entzogen hat → jetzt nur „Kanaele ansehen"): `GET #team` (versteckt, kein Grant) → **403**, `GET #regeln` (oeffentlich) → 200, `GET #beatbyte-support` (Bot hat ByteBots-Admin-Grant) → 200. Das 403 beweist, dass Discord die `@everyone`-VIEW-Sperre real erzwingt (vorher kam 200, weil der Bot temporaer **Administrator** hatte und jede Kanalsperre umging — als Nebenbefund entdeckt und behoben). Damit ist das Gating doppelt bestaetigt (Rechnung + Enforcement) und der Bot wieder minimal berechtigt. Kein Code, reine Server-/Verifikationsarbeit.
- **docs:** `CHECKLISTE.md` auf die **mehrsprachige Positionierung** nachgezogen (Folge der Website-Umstellung vom 2026-07-15, auf Nachfrage bestaetigt): i18n-Abschnitt-Intro von „‚Deutsch' bleibt USP" auf **„Positionierung ist mehrsprachig"** umformuliert; Phase-2-Punkt „‚Deutscher Bot' als Alleinstellungsmerkmal" → **„mehrsprachig (DE+EN) in jede Beschreibung"**; Phase-5-App-Directory-Hinweis „German-language music bot = Nische" → mehrsprachig positionieren; Ideen-Punkt (Verwaltungs-Bot) „deutschsprachige Nische" → „mehrsprachige Nische". Reine Doku, kein Code. **Hinweis:** die CLAUDE.md-Konvention „UI-Texte der Bots sind deutsch" bleibt gueltig, bis die Bot-i18n (eigener Checklisten-Abschnitt) umgesetzt ist — bis dahin ist der mehrsprachige Claim vorgezogen.
- **brand:** **Bot-Identität als wiederverwendbares Design-System** in `brand/` festgeschrieben. `brand/README.md` dokumentiert alle Strukturen (Design-Tokens/Farben je Bot, dunkler Tile-Grund, zentraler „/"-Kreis-Badge, Motive Equalizer/Soundboard/Schild, Avatar- 512 & Banner-960-Aufbau, Einsatzorte + API-Anwendung, „neuen Bot ergänzen"). `brand/generate.js` baut alle Avatare/Banner aus den Tokens neu (SVG immer, PNG via optionalem `@resvg/resvg-js`) — reproduziert die ausgerollten Assets **byte-identisch** (verifiziert). Dazu `brand/svg/` + `brand/png/` (je 3 Avatare + 3 Banner). Commit `bbf2bc5`. Nicht in Dockerfiles → kein Deploy noetig. Merk-Notiz: Claude-Memory `bot-identity-designsystem`.
- **music-bot:** **Web-Player-Favicon + In-App-Logos auf neues BeatByte-Avatar** (`bots/music-bot/app`). Favicon (`index.html`) sowie Sidebar-Brand + Setup-Screen (`src/App.jsx`, 2×) zeigten auf den **alten Discord-CDN-Avatar-Hash** `4764a925…`, der nach dem Avatar-Update **404** war (kaputte Bilder). Jetzt lokal via `public/beatbyte-avatar.png` eingebunden → bricht kuenftig nicht mehr bei Avatar-Aenderungen. Commit `c0a774e`, music-bot per Coolify neu gebaut (`finished`). Verifiziert: `beatbyte.bytebots.de/beatbyte-avatar.png` HTTP 200, Favicon-Ref lokal, kein toter CDN-Hash mehr. **Offen:** Tauri-Desktop-Icons (`src-tauri/icons/*`) sind noch das alte Logo.
- **infra / website:** **Neue Bot-Identitaet fuer alle 3 Bots live** (Avatar + Banner + App-Icon). Design: durchgehendes Motiv im Hintergrund — **BeatByte Equalizer**, **EarTastic Soundboard**, **Admin Schild** — mit zentralem **„/"-Kreis** als gemeinsames Wiedererkennungs-Merkmal (dunkler ByteBots-Tile, Lila/Cyan-Gradient; SVG→PNG via `@resvg/resvg-js`, 1024er). Discord per API gesetzt: `PATCH /users/@me` (Avatar+Banner) + `PATCH /applications/@me` (App-Icon), je Prod-Token (BeatByte/EarTastic aus Root-`.env`, Admin aus `ADMIN_DISCORD_TOKEN` in `.env.local`), mit 429-Retry; alle CDN-verifiziert (Avatar+Banner HTTP 200). Website: `src/assets/beatbyte-avatar.png` + `eartastic-avatar.png` ersetzt (Commit `6da7c52`, isoliert), Website per Coolify deployed (`finished`, `bytebots.de` HTTP 200) — greift ueber `BotAvatar` in Mockups/Player/Status. Ersetzt die alten Off-Brand-Avatare (Regenbogen-Equalizer / 3D-Roboter-Maskottchen mit Text). Quell-SVGs liegen in `.devtools/` (gitignored) — bei Bedarf in ein getracktes Asset-Verzeichnis uebernehmen.
- **website:** „Anmelden"-Button in der Navbar (Desktop) als **gefuellten Discord-Blurple-Button** gestaltet (`#5865F2`-Hintergrund, weisses Discord-Logo + Text, Hover `#4954e0`) statt Ghost-/Textbutton — konsistent mit dem bereits gefuellten Mobile-Login. Der Hairline-Trenner setzt ihn weiter von der gradient-„Bot einladen"-CTA ab. Vite-Build gruen.
- **website:** Navbar-Aktionsbereich (oben rechts) ueberarbeitet: die **segmentierte Utility-Leiste (Karte mit Rahmen/Segment-Trennern) entfernt** — Support, Sprache und Login sitzen jetzt als **freistehende Elemente** (einheitliche 40px-Hoehe, abgerundeter Hover), ein schlanker Hairline-Trenner setzt die **„Bot einladen"-CTA** ab. Ausserdem **doppeltes Discord-Logo aufgeloest**: der Support-Server-Button bekommt ein eigenes Icon (`ForumOutlinedIcon`), das Discord-Logo bleibt allein dem Login vorbehalten (Desktop + Mobile-Drawer). Funktion (Support-Link, Sprachwahl, Discord-Login, Avatar-Menue) unveraendert. Vite-Build gruen. Kehrt die segmentierte Leiste aus Commit `86bcf91` bewusst wieder um (Luca gefiel Karte + doppeltes Icon nicht).
- **website:** Positionierung von „deutsch" auf **„mehrsprachig"** umgestellt (de+en + SEO-Meta): Hero-Badge „Deutsche/German Discord-Bots" → „Mehrsprachige/Multilingual", Trust-Chip „Auf Deutsch/In German" → „Mehrsprachig/Multilingual", Feature-Card „Kostenlos & auf Deutsch" → „…& mehrsprachig". Konkrete Sprach-Claims **neutralisiert statt falsch behauptet** (die Bot-Commands sind weiterhin rein deutsch, kein i18n in den Bots): „deutsche Befehle/Slash-Commands" → „moderne Slash-Befehle/Slash-Commands", „deutscher Support" → „persoenlicher Support". `index.html`-Meta (Titel/Description/OG/Twitter) „(deutsch)/deutschsprachige" → „(mehrsprachig)/mehrsprachige"; ein deutsches Long-Tail-Such-Keyword (`Discord Musik Bot deutsch`) bewusst behalten. Rechtstexte (Impressum-Adresse, DSGVO, Urheberrecht) + Sprach-Label „Deutsch" bleiben. **Achtung:** steht im Spannungsfeld zur CHECKLISTE-Notiz „‚Deutsch' bleibt USP" — bewusste Entscheidung auf Nachfrage; die Bots sind aktuell noch nicht wirklich mehrsprachig.
- **docs:** `CHECKLISTE.md`: erledigte Website-Punkte abgehakt, die schon umgesetzt+committet waren (Commit `8ef29d6`): **Nutzungsbedingungen-Seite** `[x]`, **Datenschutz um die Bots ergaenzt** `[x]` (Rechtliche Seiten), **SEO-Content** „Discord Musik Bot deutsch"/„Rythm Alternative" `[x]` und **Meta-/OG-Tags** `[x]` (Ideen fuer spaeter), jeweils mit Kurz-Notiz + Datum. Bewusst offen gelassen: *Terms/Privacy-URLs im Developer Portal eintragen* (manuelle Portal-Aktion, URLs stehen bereit) und *Demo-Video/GIF* (nicht aus dem Repo machbar).
- **infra:** **Kanal-Gating fuers Onboarding nachgeruestet** (auf Nachfrage — das role-basierte Onboarding von vorhin liess alle Kanaele sichtbar, die Bot-Wahl hatte also keinen Effekt). Per API (`PUT /channels/<cat>/permissions/<role>`, Skript im Scratchpad) auf den **Kategorien** 🎵 BeatByte + 🔊 EarTastic: `@everyone` VIEW verweigert, jeweilige Self-Rolle (`BeatByte 🎵`/`EarTastic 🔊`) + `Team` + `ByteBots Admin` VIEW erlaubt; die 6 Kanaele haben keine eigenen Overwrites → **erben** von der Kategorie (ein Set pro Kategorie genuegt). Wirkung per Rechte-Simulation verifiziert: neues Mitglied sieht keine Bot-Kanaele, `🎵 BeatByte`-Wahl sieht nur BeatByte, `🔊 EarTastic`-Wahl nur EarTastic, Team sieht alles. Onboarding blieb `enabled` (Discords ≥7-sichtbar/≥5-schreibbar-Regel greift nicht rueckwirkend) und Welcome-Screen aktiv. **Wichtig:** Owner/Admins umgehen Kanalrechte → echtes Testen nur mit Zweit-Account ohne Team-Rolle. Reversibel (Overwrites loeschen = Kanaele wieder oeffentlich). Die Aktion wurde vom Auto-Mode-Classifier zunaechst geblockt (Aenderung an geteilter Ressource) und erst nach ausdruecklicher Freigabe ausgefuehrt. Der Bot brauchte weiter MANAGE_ROLES → danach wieder auf read-only (nur „Kanaele ansehen") zuruecknehmen. `CHECKLISTE.md` entsprechend aktualisiert.
- **website:** Login im Header-Segment verständlicher: das kryptische Profil-Icon durch **Discord-Icon (in Discord-Blurple `#5865F2`) + Label „Anmelden"** ersetzt — Discord-Logo davor, dann der Text, in der segmentierten Utility-Leiste. Neuer i18n-Key `nav.signIn` („Anmelden" / „Sign in", de+en). Mobile-Login-Button-Icon analog von Profil- auf Discord-Icon umgestellt. Eingeloggter Zustand (Avatar + Menü) unverändert; `PersonIcon` bleibt nur noch im „Profil"-Menüpunkt. Vite-Build grün, Navbar headless geprüft.
- **infra:** **Onboarding des Support-Servers aktiviert + 2 selbstvergebbare Rollen angelegt** (Checklisten-Punkte „Onboarding aktivieren" + „Selbstvergebbare Rollen"). Per API (`PUT /guilds/1525977525400895668/onboarding`, Einmal-Skript im Scratchpad, `ADMIN_DISCORD_TOKEN`): Prompt **„Welchen Bot nutzt du?"** (Mehrfachauswahl, `in_onboarding`, mode `DEFAULT`), 2 Optionen 🎵 BeatByte / 🔊 EarTastic, die je eine neue Rolle vergeben; `default_channel_ids` = alle 11 oeffentlichen Kanaele. **Bewusst OHNE Kanal-Gating** — Server hat nur 5 oeffentliche Allgemein-Kanaele; wuerde man die BeatByte-/EarTastic-Kategorien fuers „Freischalten" verstecken, blieben <7 sichtbar und Discord lehnt Onboarding ab (Regel: neue Member muessen ≥7 Kanaele sehen / in ≥5 schreiben). Echtes Kanal-Freischalten braucht daher mehr oeffentliche Kanaele + den UI-Wizard. Neue Rollen **`BeatByte 🎵`** (`#a855f7`) + **`EarTastic 🔊`** (`#22d3ee`), pingbar, keine Extra-Rechte, Emoji im Namen → keine Kollision mit den managed Bot-Rollen `BeatByte`/`EarTastic`; Skript idempotent (Rollen nur wenn fehlend). Verifiziert via `GET /onboarding` (enabled, 1 Prompt, 2 Optionen mit korrekten `role_ids`) + `GET /roles`. Brauchte am Bot **MANAGE_GUILD + MANAGE_ROLES** (Luca temporaer gesetzt) → danach wieder auf read-only (nur „Kanaele ansehen") zuruecknehmen; Onboarding/Rollen bleiben als Server-Zustand. `CHECKLISTE.md`: Onboarding `[x]`, Rollen-Punkt teil-erledigt (`@Ankündigungen` offen). Damit sind vom Optik-Block **Willkommensbildschirm + Onboarding live**, Banner/Splash geparkt (kein Boost).
- **website:** Header-Aktionsbereich (oben rechts) strukturiert: Support-, Sprach- und Login-Icon sitzen jetzt in **einer segmentierten Leiste** (gemeinsamer Rahmen, Hairline-Trenner zwischen den Segmenten, geklippte Ecken) statt als drei lose Buttons; die **„Bot einladen"-CTA** ist mit klarem Abstand abgesetzt. Höhen/Radien vereinheitlicht (38px), Menüs aus der Leiste gelöst (portalen, kein Layout-Einfluss). Rein visuelle Umstrukturierung der `Navbar.jsx`-Desktop-Zeile; Funktion (Support-Link, Sprachwahl, Discord-Login, eingeloggtes Avatar-Menü) unverändert. Vite-Build grün, Navbar headless gerendert + geprüft.
- **infra:** **Willkommensbildschirm des Support-Servers aktiviert** (Checklisten-Punkt „Support-Server aufwerten"). Per API gesetzt (`PATCH /guilds/1525977525400895668/welcome-screen`, Einmal-Skript im Scratchpad, Token `ADMIN_DISCORD_TOKEN` aus `.env.local`): Beschreibung „Deutsche Discord-Bots — Musik mit BeatByte 🎵 und Soundboard mit EarTastic 🔊 …" + 3 Kanaele (📢 #regeln, 🎵 #beatbyte-support, 🔊 #eartastic-support), Kanal-IDs live aus `GET /guilds/.../channels` gemappt. Verifiziert: Guild-Features enthalten jetzt `WELCOME_SCREEN_ENABLED`. **Voraussetzung war** dem read-only Bot „ByteBots Admin" einmalig „Server verwalten" zu geben (Luca hat ihm kurzzeitig alle Rechte gegeben) — **Empfehlung: wieder auf read-only zuruecksetzen**, der Welcome-Screen bleibt als Server-Zustand erhalten. `CHECKLISTE.md`: Punkt auf `[x]` gesetzt. Onboarding bleibt bewusst ein UI-Wizard-Schritt (Discord erzwingt Mindest-Kanaele + verdrahtet die Kanal-Rechte automatisch — Vorlage in `.devtools/support-server-setup.md` §3). Banner/Splash weiter geparkt (kein Boost gewuenscht).
- **music-bot:** **Audio-Filter greifen jetzt auch im Web-Player sofort auf den laufenden Song.** Bug: Der API-Endpoint `POST /api/guild/:id/filter` hat nur `queue.filter` gesetzt, aber den aktuellen Track NICHT neu gestreamt (der Code-Kommentar behauptete faelschlich „restarts current song") → im Web-Player war Bassboost/Nightcore erst beim naechsten Song hoerbar, waehrend der Discord-`/filter`-Command sofort wirkte. Fix: Die Re-Stream-Logik (Stream ab aktueller Position mit Filter neu starten) in einen geteilten Helfer `restartCurrentWithFilter(queue)` in `index.js` gezogen und ueber `ctx` exponiert; `/filter`-Command und API-Handler rufen jetzt beide denselben Helfer → koennen nicht mehr auseinanderlaufen. `commands/filter.js` entsprechend entschlackt (nicht mehr genutzte Imports `createAudioResource`/`StreamType`/`killQueueProcesses` entfernt). Helfer ist ein No-op, wenn nichts laeuft (Filter greift dann beim naechsten Song) und sendet Fehler nur, wenn ein Text-Channel gebunden ist (API-Kontext hat keinen). Live getestet ueber die Admin-API gegen den Dev-Bot: `elapsed` springt beim Filterwechsel zurueck (= Song startet neu), Filter-State korrekt, ungueltiger Filter weiterhin 400; `node --check` auf allen drei Dateien gruen.
- **website:** Navbar oben rechts aufgeraeumt: (1) **Sprach-Flagge repariert** — statt externem `flagcdn.com`-Bild (brach durch Ad-Blocker/Netz/CSP → kaputtes Icon) jetzt lokal gebuendelte SVGs (`assets/flag-de.svg`, `flag-gb.svg`), rendern immer scharf, kein externer Request. (2) **Login entschlackt**: der lange „Mit Discord anmelden"-Button (mit zweitem Discord-Logo direkt neben dem Support-Discord-Button) durch ein kompaktes **Profil-Icon** ersetzt (`PersonIcon`, Klick → Discord-OAuth `DISCORD_LOGIN_URL`), im Stil der uebrigen Icon-Buttons; Mobile-Login-Icon analog. Der Support-Server-Button bleibt (jetzt einziges Discord-Logo). Eingeloggter Zustand (Avatar + Menue) unveraendert. Vite-Build gruen, Navbar headless gerendert + geprueft.
- **infra:** Checklisten-Punkt *„Support-Server aufwerten → Optik"* vorbereitet (alle 3 Teilpunkte). Ist-Zustand mit `npm run inspect:discord` ausgelesen: Server hat **0 Boosts** → **Banner + Invite-Splash technisch gesperrt** (Discord gibt Splash erst ab Boost-Level 1 / 2 Boosts, Banner ab Level 2 / 7 Boosts frei; Feature-Liste `NEWS,AUTO_MODERATION,COMMUNITY` bestaetigt: kein `BANNER`/`INVITE_SPLASH`). Trotzdem **beide Grafiken im ByteBots-Look fertig gebaut** (`.devtools/banner-bytebots.svg`+`.png` 960×540, `splash-bytebots.svg`+`.png` 1920×1080; Splash haelt die Mitte dunkel fuer die Einladungs-Karte) — Violett→Cyan-Marken-Gradient, Equalizer+Wellen-Motiv (Musik+Sound), `</>`-Slash-Wasserzeichen, mehrfarbige Punkte; passt zum Set `banner-beatbyte/eartastic/admin`. SVG→PNG mit `qlmanage` gerendert + per `sips` auf exaktes Format gecroppt (kein `librsvg` installiert). **Onboarding + Willkommensbildschirm** brauchen keinen Boost → fertige Copy-&-Paste-Vorlagen (Frage/Optionen/Kanal-Freischaltung bzw. Beschreibung+3 Kanaele) in **`.devtools/support-server-setup.md`**. `CHECKLISTE.md`: die 3 Punkte mit Status/Boost-Blocker + Verweis auf Vorlage annotiert. Alles in `.devtools/` (gitignored) → landet in keinem Image; Onboarding/Welcome selbst sind Discord-UI-Aktionen (nur du), Banner/Splash warten auf Boosts.
- **website:** Website-Punkte aus der Checkliste umgesetzt (Rechtliches + SEO). **Neue Nutzungsbedingungen-Seite** `/nutzungsbedingungen` (`pages/Nutzungsbedingungen.jsx`, 10 Abschnitte: Leistung, Voraussetzungen, Pflichten, Nutzer-Inhalte/Uploads, Verfuegbarkeit, Haftung, Datenschutz-Verweis, Aenderungen, Schlussbestimmungen, Kontakt; Route in `App.jsx` + Footer-Link, i18n de+en). **Datenschutz §6 konkretisiert**: welche Daten BeatByte/EarTastic wirklich speichern (aus den DB-Schemata verifiziert — Playlists/Historie/Likes/Follows + Guild-Settings; hochgeladene Sounds/Favoriten/Volume; Web-Login via Discord-OAuth), inkl. Hosting (Hetzner/DE), Rechtsgrundlagen und Loeschung; als Liste + Footer gerendert (de+en). **Meta-/OG-/SEO** in `index.html`: keyword-reicher Titel/Description (u.a. „Discord Musik Bot deutsch", „Rythm Alternative"), `keywords`/`author`/`canonical`, vollstaendige Open-Graph-/Twitter-Tags mit **absoluter** Bild-URL + `summary_large_image` + `og:locale`. **Neues OG-Banner** `public/og-image.png` (1200×630, Marken-Look, via Headless-Chrome gerendert) fuer huebsche Discord-Link-Vorschauen. **SEO-Hygiene**: `public/robots.txt` + `public/sitemap.xml` (12 Routen). Vite-Build gruen, neue Seiten + Assets (HTTP 200) headless geprueft. Nicht aus dem Repo machbar (offen): Demo-Video/GIF; Terms/Privacy-URLs im Discord Developer Portal eintragen (jetzt `bytebots.de/nutzungsbedingungen` bzw. `/datenschutz`).
- **docs:** `CHECKLISTE.md`: Mehrsprachigkeit der Bots aus „Ideen fuer spaeter" in einen eigenen aktiven Abschnitt **„Mehrsprachigkeit (i18n)"** (zwischen „Technik & Robustheit" und „Bewerbung & Wachstum") hochgezogen und in konkrete Teilaufgaben zerlegt: i18n-Ebene fuer Bot-Antworten (Sprachdateien wie Website `de.js`/`en.js`, Fallback DE), Discord-Command-Lokalisierung (`setNameLocalizations`/`setDescriptionLocalizations`), Sprachwahl pro Server/Discord-Locale, Englisch zuerst, Website-i18n analog. „Deutsch" bleibt USP.
- **infra:** **Alle 3 Apps auf Prod deployed** (Coolify-API, sequenziell wegen RAM-Enge). Damit sind die zuletzt gepushten Website-Aenderungen live: Support-Server-Link in Footer/Navbar (`916f305`) und die Bot-Detailseiten mit gerenderten Mockups (`4d63666`) — das war der einzige offene Deploy (Bots waren schon aktuell). Deployment-UUIDs `eisbner…` (website), `qzi3bo…` (music-bot), `de4lve…` (soundboard-bot), alle `finished`. Verifiziert: `bytebots.de` HTTP 200, `beatbyte.bytebots.de/status` HTTP 200, `soundboard.bytebots.de/api/health` `{status:ok}` mit frischer Uptime (Container neu gestartet). Commit `034d767` (Changelog + Marketing-Copy `marketing/support-server.md` + `topgg-beatbyte.md` + `dbl-background-beatbyte.png`) auf `coolify-deploy` gepusht — reine Copy/Docs, landet in keinem Image.

## 2026-07-14

- **infra:** **Profilbild fuer den ByteBots-Admin-Bot** gesetzt (vorher keins). Design „Schild-Rahmen" im ByteBots-Stil (dunkler Radial-Tile, Gradient-Schild-Umriss `#a855f7→#d946ef→#22d3ee` mit Slash + Block-Cursor aus Logo-Variante 1c) — signalisiert „Verwaltung", bleibt Teil der Familie. Aus 3 Entwuerfen (Artifact-Vorschau) gewaehlt, SVG→PNG via `@resvg/resvg-js` (1024×1024) gerendert und an **beiden** Stellen gesetzt: **Bot-Avatar** (`PATCH /users/@me`, Chats/Mitgliederliste) **und App-Icon** (`PATCH /applications/@me`, Developer-Portal/OAuth-„Einladen"-Screen/App-Directory — vorher „BA"-Platzhalter), beide mit `ADMIN_DISCORD_TOKEN`. Discord-CDN liefert beide aus (HTTP 200, Hash `6ca8a8c2…`). Quelle liegt in `.devtools/bytebots-admin-avatar.svg` (gitignored) — bei Bedarf in ein getracktes Asset-Verzeichnis verschieben.
- **infra:** `/support` + `/invite` **auf Prod deployed** (Grundlagen). Command-Registrierung per API: **BeatByte 25 global**, **EarTastic 7 guild-scoped** in Guild `879650782629199893` (bestehendes Modell getroffen — EarTastic-Commands sind dort guild-scoped, nicht global → Duplikate vermieden). Danach beide Bot-Container per Coolify-API neu gebaut (`music-bot` + `soundboard-bot`, sequenziell wegen RAM-Enge, beide Deployments `finished`), damit die neuen Handler (`support.js`/`invite.js`/`libs/links.js`) im Image sind. Verifiziert: API-Command-Liste enthaelt `/support`+`/invite`, `beatbyte.bytebots.de/status` HTTP 200, `soundboard.bytebots.de/api/health` `{status:ok}` mit frischer Uptime (Container neu gestartet, `require('../../../../libs/links')` laeuft in Prod). Commits `b09b365` (bots), `916f305` (website), `c1c28a0` (docs) auf `coolify-deploy` gepusht. **Offen:** EarTastic global schalten (falls gewuenscht), Website-Deploy fuer den Support-Link im Footer/Navbar.
- **infra:** Englische Marketing-Texte fuer den **ByteBots Support-Server** verfasst (`marketing/support-server.md`, Companion zu `topgg-beatbyte.md`): Headline (+ 3 Alternativen) und lange Markdown-Beschreibung fuer Server-Listings. Deckt beide Bots ab (BeatByte 🎵, EarTastic 🔊), Fokus auf Support/Feature-Requests/Announcements/Community; Invite `discord.gg/F5UdSh5NnY`, Links `bytebots.de` / `beatbyte.bytebots.de` / `soundboard.bytebots.de`. Reine Copy-Datei, wird von keinem Dockerfile ins Image kopiert.
- **music-bot / soundboard-bot:** `/invite` auf **Umbrella-Design** umgebaut — Titel „➕ ByteBots einladen" (statt bot-spezifisch), listet jetzt **alle drei Bots** als Link-Buttons: BeatByte 🎵, EarTastic 🔊, ByteBots Admin 🛡️ *(in Entwicklung)* + Support-Server 💬, ByteBots-Lila `0x6E41CC` in beiden Bots. Neue Konstante `ADMIN_INVITE` in `libs/links.js` (ByteBots Admin ist `bot_public:true`, Client `1525999463615827999`; minimale Rechte `permissions=1024` — beim Feature-Ausbau zum Verwaltungsbot erhoehen). Getestet: `node --check` + Verhaltenstest (4 Buttons, Admin-Client-ID, alle `https`). Auf den Dev-Bots neu registriert + Prozesse neu gestartet; Prod-Deploy weiterhin ausstehend.
- **website:** Leere Screenshot-Platzhalter auf beiden Bot-Detailseiten durch gerenderte Live-Mockups ersetzt (keine echten Screenshots noetig). Neue `components/AppMockups.jsx` mit browser-gerahmten Web-App-Nachbauten: `WebPlayerMock` (BeatByte-Player mit Sidebar/Now-Playing/Queue) und `DashboardMock` (EarTastic-Dashboard mit Kategorie-Chips/Sound-Grid/Upload). `DiscordMockups.jsx` um kompakte Feature-Embeds erweitert (`MiniEmbed`-Baustein + `QueueMock`, `PlaylistMock`, `FilterMock`, `AutoDjMock`, `UploadMock`, `FavoritesMock`). MusicBot: Web-App-Slot → `WebPlayerMock`, Galerie → 4 Feature-Embeds. SoundboardBot: Discord-Vorschau → `EarMockup`, Dashboard-Slot → `DashboardMock`, Galerie → Upload/Favoriten. Video-Platzhalter + Label-Kachel-Raster entfernt, ungenutzte Icon-Imports (`ImageIcon`/`PlayCircleIcon`) raus. Vite-Build gruen, beide Seiten headless gerendert + visuell geprueft.
- **music-bot / soundboard-bot:** Neue Slash-Commands **`/support`** und **`/invite`** in beiden Bots. `/support` verlinkt den ByteBots Support-Server + Website; `/invite` die Bot-OAuth-Invites (eigener Bot zuerst, Cross-Promo zum jeweils anderen) + Support-Server. Beide antworten **ephemer** mit Embed (BeatByte Lila `0x6E41CC` / EarTastic Cyan `0x22D3EE`), Link-Buttons und `bytebots.de`-Footer. Geteilte Link-Konstanten in neuer **`libs/links.js`** (von beiden Bots via `../../../../libs/links` requiret, wird wie die uebrigen `libs/` von den Dockerfiles mitkopiert; Website behaelt ihre eigene Kopie in `website/src/config.js`). Getestet: `node --check`, `data.toJSON()` und ein Verhaltenstest (`execute()` → valide Reply-Payload, alle Button-URLs `https`) fuer alle 4 Commands. **Prod-Deploy noetig, damit sie erscheinen:** `npm run deploy` in `bots/music-bot` (GLOBAL — dafuer `MUSIC_GUILD_ID` vorher NICHT gesetzt lassen) und in `bots/soundboard-bot`. Command-Zahl danach: music 25, soundboard 7.

## 2026-07-13

- **website:** Support-Server-Invite (`discord.gg/F5UdSh5NnY`) auf der Website eingebaut (Grundlagen-Punkt aus CHECKLISTE): neue Konstante `SUPPORT_INVITE` in `src/config.js`; Footer bekommt einen externen „Support"-Link (Router-Map um `external`-Fall erweitert → `<a target=_blank rel=noopener>`); Navbar-Desktop einen Discord-Icon-Button (read-only Support, visuell abgesetzt vom blauen Login-Button) und das Mobile-Drawer einen „Support"-Button. Label `nav.support` in `i18n/de.js` + `en.js` („Support"). Vite-Build gruen, Invite-URL im Bundle verifiziert.
- **infra:** Alle 3 Apps per Coolify-API deployed (music-bot, soundboard-bot, website) vom Branch `coolify-deploy`, Commit `3514e54` — damit sind die Dev-Test-Runner, der Server-Inspektor, die lokalen Dev-Schalter und die BeatByte-Detailseite (echtes Now-Playing-Embed) live. Alle 3 Deployments `finished`; verifiziert: `beatbyte.bytebots.de/status` HTTP 200, `soundboard.bytebots.de/api/health` `{status:ok, sounds:1}` (frische Uptime ~3 min), `bytebots.de` HTTP 200.
- **infra:** Sicherheits-Haertung des Server-Inspektors **abgeschlossen**: eigener read-only Bot **„ByteBots Admin"** (Bot-User-ID `1525999463615827999`, nur „Kanaele ansehen"/`permissions=1024`) im Developer Portal angelegt, auf den Support-Server eingeladen und sein Token in `.env.local` als `ADMIN_DISCORD_TOKEN` hinterlegt. Damit ist der zuvor dort liegende **Prod-BeatByte-Token ueberschrieben** — es liegt kein Prod-Token mehr lokal (die Root-`.env` als Master-Vorlage bleibt unveraendert). Verifiziert: `npm run inspect:discord` loggt sich als „ByteBots Admin" ein (nicht mehr `BeatByte`, keine Prod-Warnung) und liest den Server. Zusatz: das Skript warnt weiterhin, falls es je mit einem Prod-Token laeuft; `.env.local.example` dokumentiert das Setup inkl. Invite-URL. Siehe [[discord-support-server-zugriff]].
- **infra:** Read-only Server-Inspektor `scripts/discord-inspect.js` ergaenzt — liest den ByteBots-Support-Server (Guild `1525977525400895668`) per Discord-REST-API v10 aus: Server-Overview (Mitglieder, Boosts, Verifizierung, Vanity, Icon/Banner, Features, Community-Status), Kanalbaum nach Kategorien inkl. Forum-Tags/Topics, Rollen und Onboarding-/Welcome-Screen-Status. Kein Gateway/keine privilegierten Intents noetig. Token aus `.env.local` via `ADMIN_DISCORD_TOKEN` (bewusst OHNE `MUSIC_`/`SOUNDBOARD_`-Praefix → `libs/loadEnv.js` mappt es nicht, die Bots sehen es nie), Guild via `SUPPORT_GUILD_ID`. Slot in `.env.local`(.example) + npm-Script `inspect:discord` ergaenzt; Skript liegt unter `scripts/` und wird von keinem Dockerfile ins Image kopiert. Getestet: Fehlerpfade (kein Token → Anleitung, ungueltiger Token → 401, kein Guild-Zugriff → 403/404) und voller Report gegen `DevBotServer`. **Zugriff erreicht:** Der Support-Server wurde erfolgreich ausgelesen (eingeloggt als Prod-`BeatByte` `1488919318472298647`). Weg dorthin: die lokalen `MUSIC_`/`SOUNDBOARD_`-Tokens gehoeren zu den **Dev**-Bots (`BeatByteDev`/`EarTasticDev`, nur im `DevBotServer` — Nachweis `GET /users/@me/guilds`); Coolify-API gibt Env-Werte nie zurueck; SSH zum Prod-Host wurde vom Auto-Mode-Classifier blockiert. Funktioniert hat: den echten Prod-Token aus der gitignoreten Root-`.env` (**Music-Block [1] `DISCORD_TOKEN`**, dieser Bot ist Mitglied des Support-Servers) nach `ADMIN_DISCORD_TOKEN` in `.env.local` kopiert (per Node, Wert nie ausgegeben). **Sicherheitshinweis:** Damit liegt aktuell der Prod-Bot-Token in `.env.local` (gitignored) — sauberer waere ein eigener read-only "ByteBots Admin"-Bot; dann `ADMIN_DISCORD_TOKEN` darauf umstellen. Merk-Notiz in Claude-Memory [[discord-support-server-zugriff]]. Ist-Zustand des Servers zum Zeitpunkt: 16 Kanaele (INFO/BEATBYTE/EARTASTIC/COMMUNITY), 4 Rollen (Team, Bots + 2 Bot-Rollen), Community aktiv, EarTastic bereits auf dem Server, aber Onboarding/Welcome-Screen AUS, keine Forum-Kanaele, kein `#willkommen`/`#status`, ~3 Mitglieder.
- **website:** BeatByte-Detailseite (`pages/MusicBot.jsx`): Der „Discord Preview"-Block zeigte faelschlich das Web-Player-Widget (`MusicPlayer`) statt eines echten Discord-Embeds — jetzt das echte Now-Playing-Embed, identisch zu dem auf der Bots-Uebersicht. Dazu `BeatMockup`/`EarMockup` samt Discord-Primitiven (`DiscordMsg`, `DcBtn`, Farb-Tokens) aus `pages/Bots.jsx` in eine geteilte `components/DiscordMockups.jsx` extrahiert (DRY: beide Seiten rendern exakt dasselbe Embed). `MusicPlayer` wird dadurch nirgends mehr verwendet (toter Code). Vite-Build gruen.
- **bots:** Lokale Dev-Test-Runner `bots/*/dev-test.js` ergaenzt — booten den echten Bot (require `src/index.js`) und fahren JEDEN Slash-Command gegen den echten `ctx`/`db` mit faithful Mock-Interaction (echte Guild/Channel-Objekte aus dem Client-Cache), inkl. echter Voice-Verbindung. Ergebnis: **music 31/31 gruen inkl. echtem `/play`-Streaming** (`/join` → `ready`, Player 3,5s durchgehend `playing`) und **soundboard 13/13 gruen inkl. echter `/sound`-Wiedergabe** (selbst erzeugter Stille-WAV → ffmpeg-static → Opus → Voice). Beide `index.js` exportieren dafuer `client`/`ctx` (Kommentar erklaert: inert in Prod, da index.js dort nur Entrypoint ist). Die Runner liegen bewusst NICHT unter `src/` → werden von den Dockerfiles nicht ins Image kopiert. Fuer `/play` lokal: das gebuendelte yt-dlp (youtube-dl-exec) braucht Python 3.10+, lokal ist nur 3.9 da → offizielles standalone yt-dlp (2026.07.04) nach `.devtools/yt-dlp` geladen (gitignored) und per `MUSIC_YTDLP_PATH` eingebunden; mit `YTDLP_PROXY=direct` streamt YouTube lokal ueber die Heim-IP.
- **music-bot:** Zwei prod-neutrale lokale Schalter in `src/index.js`: `YTDLP_PROXY=direct|none|off|local` laesst yt-dlp OHNE Proxy/POT ueber die lokale IP laufen (die Coolify-Aliasse `warp:1080`/`pot-provider:4416` gibt es nur im Server-Netz); `YTDLP_PATH` erlaubt ein explizites yt-dlp-Binary (fuer den Fall, dass System-yt-dlp fehlt/zu alt ist). Beide in Prod ungesetzt → Verhalten unveraendert, die dreifach abgesicherte YouTube-Pipeline bleibt intakt. In `.env.local`(.example) dokumentiert.
- **soundboard-bot:** `SOUNDBOARD_SESSION_SECRET` in die lokale `.env.local`(.example) aufgenommen (der Code liest `SESSION_SECRET`, Fallback `API_KEY` — `web/server.js`); eigener Zufallswert fuer die Dashboard-Session-Signatur.
- **infra:** Beide Dev-Bots (`BeatByteDev`, `EarTasticDev`) end-to-end live getestet: Login, Command-Registrierung (23 + 5, guild-scoped), API-Rueckabgleich 1:1 mit Code, HTTP-Flaechen (`/status`, `/api/health`), und alle `execute()`-Pfade gegen echten Runtime. Fehlende `SOUNDBOARD_CLIENT_ID` in `.env.local` aus dem Token abgeleitet und ergaenzt.
- **infra:** Hetzner-Server aufgeraeumt — Platte war bei 83 % (30/38 GB). `docker builder prune -af` (7,44 GB Build-Cache) + `docker image prune -af` (12,16 GB ungenutzte Images) per SSH ausgefuehrt → ~18 GB frei, Platte jetzt bei **32 %** (12/38 GB). Alle 11 Container liefen durch, nur ungenutzte Layer/Images geloescht (bekannte Build-Cache-Falle). Anmerkung: RAM bleibt der Engpass (1,9 GB total, swappt bereits ~780 MB) — nur per Server-Upgrade loesbar, nicht per Cleanup.
- **website:** Favicon durch das offizielle ByteBots-Logo (Variante **1c „Slash-Befehl"** aus dem Claude-Design-Projekt „ByteBots Website Redesign") ersetzt — vorher war es noch der alte blaue Roboter-Kopf. Kanonischen Vektor `assets/bytebots-logo.svg`/`logo-c-slash.svg` aus dem Design per Design-MCP importiert und als `public/favicon.svg` abgelegt (radialer Tile-Hintergrund + Gradient-Slash/Byte-Cursor, Ring `stroke-width` 5). Daraus die PNG-Fallbacks gerendert: `bytebots-favicon.png` 512×512 (OpenGraph/Twitter), `bytebots-favicon-180.png` (apple-touch), `bytebots-favicon-32.png` (Tab). `index.html`: SVG als primäres Icon (skaliert scharf) + PNG-Fallbacks, `sizes`-Angaben korrigiert. `BrandLogo` in `src/components/ui.jsx` an die kanonische Spec angeglichen (Ring `strokeWidth` 6 → 5), damit In-Page-Logo und Favicon pixelgleich sind. Die ungenutzten alten Roboter-Assets `public/favicon.png` + `public/logo.png` bleiben vorerst liegen (nirgends referenziert).

## 2026-07-12

- **bots:** Env-Dateien konsolidiert — lokale Dev-Config jetzt in **einer** Root-`.env.local` (gitignored, committete Vorlage: `.env.local.example`): Variablen mit Präfix `MUSIC_`/`SOUNDBOARD_`, die neue `libs/loadEnv.js` mappt sie beim Start auf die echten Namen. Bereits gesetzte Umgebungsvariablen (= Coolify/Prod) gewinnen immer; eine `bots/<bot>/.env` wird als Ausnahmefall weiter gelesen. Gelöscht: die zwei lokalen `bots/*/.env`, beide `bots/*/.env.example` und die tote `bots/music-bot/.env.production` (wurde von nichts mehr gelesen, `.dockerignore` blockte sie ohnehin). `.gitignore` um `.env.local` ergänzt, CLAUDE.md angepasst. Getestet: Präfix-Mapping beider Bots, Coolify-Vorrang, `npm run deploy` beider Bots läuft bis zur Discord-API (sauberer 401 mit Platzhalter-Token).
- **infra:** Alle 3 Apps per Coolify-API deployed (music-bot, soundboard-bot, website) — damit sind SQLite-Backups/atomares Schreiben, yt-dlp-Startup-Update, Soundboard-Fehler-Handler, beatbyte-Umstellung und Website-Redesign live. Verifiziert: beide Bots online, `/status` + `/api/health` + Website antworten, Entrypoint-Update lief (yt-dlp 2026.7.4). Dabei in Coolify aufgeräumt: tote Alias-Domain `app.bytebots.de` aus den music-bot-Domains entfernt und `ALLOWED_ORIGINS` auf `beatbyte.bytebots.de,bytebots.de` gekürzt; Root-`.env`-Vorlage ebenfalls auf beatbyte-Werte gezogen.
- **website:** Bot-Mockups auf der Bots-Seite bilden jetzt die ECHTEN Discord-Nachrichten nach (Specs per Code-Analyse aus beiden Bots extrahiert): BeatByte-Now-Playing 1:1 wie `buildNowPlayingEmbed` (Farbe `#6E41CC`, Author-Zeile „Now playing" mit Mini-Avatar, Titel als Heading in der Description, textbasierter 20-Segment-Fortschrittsbalken `━●─` mit Inline-Code-Zeiten — tickt animiert wie das echte 10s-Update, Subtext `@Luca · 📋 5 in Queue`, Quadrat-Cover als Thumbnail rechts, Buttons `[⏸️|⏭️|⏹️]` + `[🔀|🔁|🎧 Web Player↗]` in echten ButtonStyles); EarTastic 1:1 wie `buildSoundboardPanel` (blurple Embed „Soundboard - Vorgegebene Sounds" mit Footer „Seite 1/1 | 8 Sounds", Kategorie-Select, Sound-Buttons grau/Favorit blurple, Nav `[◀|▶|⏹ Stop|🔄 Aktualisieren]`, Ephemeral-Hinweis). Discord-Dark-Theme-Farben (`#313338`/`#2b2d31`, 32px-Buttons, 3px-Radius).
- **infra:** Lokale Dev-`.env` für beide Bots angelegt (gitignored, Vorlage `.env.example`): Ports, frische API-Keys, lokale CORS/OAuth-Redirect-Werte, Freesound-Key aus der Root-`.env` übernommen (Spotify-Keys stehen dort nicht, nur in Coolify). Discord-Dev-Token/Application-ID/Test-Server-ID müssen noch eingetragen werden (`HIER-EINTRAGEN`-Platzhalter). Bekannte Lücke: `/play` gegen YouTube funktioniert lokal nicht, weil die Code-Defaults `warp:1080`/`pot-provider:4416` nur im Coolify-Netz existieren und der Code keinen „ohne Proxy"-Wert kennt.
- **website:** Bots-Seite komplett neu gestaltet: volle Produkt-Rows statt kompakter Karten — abwechselnd links/rechts, mit Discord-Nachrichten-Mockups (BeatByte: animiertes Now-Playing-Embed mit Cover, Fortschritt, Player-Buttons, EQ; EarTastic: Soundboard-Panel mit 8 Sounds + /sound-Eingabe), Feature-Checklisten, Akzent-Halos und Seiten-Glows. Avatare überall rund (BotAvatar auf 50 % + Akzent-Ring, auch Produktseiten-Heros). **Echte Live-Status-Daten**: neuer Hook `useLiveStatus` prüft im Browser `beatbyte.bytebots.de/status` (Music-Bot, CORS für bytebots.de erlaubt) und `soundboard.bytebots.de/api/health` (JSON mit uptime + sounds) — Status-Seite zeigt Live-Zustand, Latenz, Uptime/Sound-Zahl, Banner reagiert (grün/gelb/prüfend), „Neu prüfen"-Button; Bots-Seite zeigt Live-Online-Chips. Fake-Zeile „API Server" (api.bytebots.de existiert nicht im DNS) entfernt.
- **bots:** SQLite-Absicherung: DBs werden atomar geschrieben (erst `.tmp`, dann rename — Crash mitten im Write korrumpiert nichts mehr) und täglich nach `data/backups/` gesichert (7 rotierend); neue geteilte `libs/dbFile.js`. Soundboard-Save jetzt debounced (1s) statt Voll-Export inkl. Audio-BLOBs bei jedem Write; Shutdown flusht per `saveNow()`.
- **soundboard-bot:** Prozess-Level-Fehler-Handler nachgerüstet (`unhandledRejection`/`uncaughtException`/Client-Error, mit DB-Flush) + `.catch()` am Discord-Login — der Bot konnte bisher unter Node 22 still sterben.
- **music-bot:** yt-dlp wird jetzt beim Container-Start per pip aktualisiert (`docker-entrypoint.sh`) — das bisherige `yt-dlp -U` im Code ist bei pip-Installationen wirkungslos, Prod lief immer mit dem yt-dlp-Stand des letzten Docker-Builds.
- **infra:** CI läuft jetzt bei jedem Push auf `coolify-deploy` (vorher nur bei PRs gegen `main` — also faktisch nie) und mit Node 22 wie die Prod-Container. Der allererste echte CI-Lauf deckte direkt auf, dass `pnpm install --frozen-lockfile` seit pnpm 10 an blockierten Postinstall-Scripts scheitert — Build-Freigaben in `pnpm-workspace.yaml` repariert (dort lag eine halbfertige `allowBuilds`-Sektion mit Platzhaltertext).
- **docs:** `CLAUDE.md` angelegt: Projekt-Kontext für Claude-Code-Sessions (Struktur, Lockfile-Falle pnpm/npm, Deploy-Regeln, GUILD_ID-Warnung, YouTube-Pipeline, Konventionen).
- **website:** Echte Discord-Bot-Avatare eingesetzt: als PNG lokal gebundelt (`src/assets/beatbyte-avatar.png`, `eartastic-avatar.png`) statt Discord-CDN-URLs (Hash-URLs brechen bei Avatar-Wechsel). Neue `BotAvatar`-Komponente in `ui.jsx` (Akzent-Ring + Glow); eingesetzt auf Home-Teasern, Bots-Karten und Status-Zeilen, CDN-Verweise in MusicBot/SoundboardBot/Profile/MusicPlayer ersetzt.
- **infra/music-bot:** Web-Player-Subdomain von `app.bytebots.de` auf **`beatbyte.bytebots.de`** umbenannt. Coolify: Domains auf `beatbyte.bytebots.de,app.bytebots.de` gesetzt (alte Subdomain bleibt übergangsweise als Alias), Env-Vars `OAUTH_REDIRECT_URI`/`FRONTEND_URL`/`APP_URL` auf beatbyte umgestellt, `ALLOWED_ORIGINS` enthält vorerst beide Subdomains + bytebots.de. Alle Code-Verweise ersetzt (Website-Seiten, i18n, music-bot `.env.production`/`.env.example`, Web-Player-Button in `src/index.js`). Manuell nötig: Cloudflare-A-Record `beatbyte` → 167.233.237.112 (proxied) + neue Redirect-URI `https://beatbyte.bytebots.de/api/auth/discord/callback` im Discord Developer Portal.
- **website:** Komplettes Redesign nach Claude-Design-Projekt „ByteBots Redesign" umgesetzt: neues Design-System in `theme.js` (Space Grotesk als Display-Font, Hintergrund `#08080b`, Karten mit Gradient + 18px-Radius, Cyan als EarTastic-Akzent), neues Inline-SVG-Logo, Navbar/Footer neu (Gradient-Underline, Status-Link, „Bot einladen"-CTA), Home mit animiertem Slash-Command-Mockup im Hero, Bots-Seite mit Produkt-Karten inkl. Mini-Player/Soundboard-Preview, Commands-Seite durchsuchbar + filterbar (28 echte Commands), Guide als 3-Schritte-Layout mit Setup/Troubleshooting-Karten, Status mit Puls-Banner. Alle neuen Texte in `de.js` UND `en.js`; Routen, i18n-Mechanik, Impressum/Datenschutz und Produktseiten (MusicBot/SoundboardBot) unverändert. Neue Shared-Komponenten in `src/components/ui.jsx`.
- **infra/music-bot:** Slash-Commands von guild-scoped auf **global** umgestellt, weil der Bot jetzt auf mehreren Servern läuft. `GUILD_ID` aus dem music-bot-Block der Root-`.env` entfernt (Coolify-Env hatte sie nie), alle 23 Commands global registriert und die alten Guild-Commands auf `879650782629199893` gelöscht (sonst Duplikate). Registrierung läuft manuell: `npm run deploy` in `bots/music-bot`.
- **music-bot:** Kick respektieren + Auto-Leave, wenn der Bot allein im Voice-Channel ist.
- **soundboard-bot:** Discord-Login fürs Dashboard statt API-Key-Pflicht (Guild-Mitglieder dürfen schreiben, X-API-Key bleibt Admin-Fallback).
- **music-bot:** Webapp-Cover repariert + Discover auf tagesaktuelle Charts.
- **music-bot:** Queue-Tracks sofort vorladen, Quadrat-Cover im Queue-Embed, Live-Fortschrittsbalken im Now-Playing-Embed.

## 2026-07-11

- **music-bot:** YouTube-Wiedergabe trotz IP-Block: yt-dlp über WARP-Proxy + POT-Provider + EJS (Details in der Coolify-Deployment-Notiz).
- **music-bot:** Now-Playing-Embed überarbeitet (Quadrat-Cover mit iTunes-Fallback, Link-Button-Fix, Nachricht bleibt 24h stehen), Skip-Prefetch, Auto-DJ-Shuffle.
